#include <stdio.h>

/**
 * main - Entry
 * Description: A program that us printf
 * Return: 0
*/

int main(void)
{

	char la;

	for (la = 'a'; la <= 'z'; la++)
		putchar(la);

	for (la = 'A'; la <= 'Z'; la++)
		putchar(ch);

	putchar('\n');

	return (0);
}
